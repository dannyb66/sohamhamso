---
import { safeJsonForScript } from '../../lib/safe-json';

interface Props {
  nodes?: Array<Record<string, unknown>>;
}

const { nodes = [] } = Astro.props;
---

{nodes.map((node) => (
  <script type="application/ld+json" set:html={safeJsonForScript(node)} />
))}
